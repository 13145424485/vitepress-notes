## ** RAG 怎么实现的，流程是什么样的**



可以理解成“先查资料，再让模型回答”，而不是只让模型凭自己生成。
它的大致流程是：

第一步，把知识库内容切分成一段一段。
第二步，把这些内容做向量化，存到向量库里。
第三步，用户提问后，先把问题也转成向量，去知识库里检索最相关的内容。
第四步，把检索出来的内容连同用户问题一起交给大模型。
第五步，大模型基于这些上下文生成回答。

# 索引构建

## 文本分块，大文本分块处理？

**文本分块**本质上就是把一篇很长的文档**拆成适合做向量检索的小片段**，因为如果一整篇都拿去做 embedding（嵌入），一方面向量语义太杂，另一方面检索命中后返回给大模型的上下文也会太长，不利于准确回答。我们项目里是先按 Markdown 的标题做一次结构化切段，尽量保证一个片段内部语义完整；如果某一段还是太长，再按固定长度继续切，比如控制在 800 个字符以内，同时保留一部分重叠内容，我们现在是 100 个字符重叠，这样可以避免一句话刚好被切断，影响上下文连续性。

简单说就是**先按文档结构粗切，再按长度细切**，目标是既保证语义完整，又保证检索粒度合适。



- 引入 NLP 处理
- 固定 or 文本段分块
- 文档结构化分块（标题、段落）





## 文档解析是怎么做的？

### 图文文档如何存储检索？

​    如果是图文文档的 RAG，我一般不会把它简单当成一段文本去做向量化，而是会先把原始文件放对象存储，再把**文档解析成**段落、表格、图片、caption（图片中的文字信息） 和版面位置信息，文本、表格文本、OCR 结果、图片语义描述**分别建索引**；检索的时候也不是只走一条向量召回，而是文本向量、关键词检索、图片或图表相关索引**一起召回**，再做 rerank，最后按章节和页内结构把上下文拼回去给大模型。---这样做的原因很直接，图文 RAG 的难点不在“存下来”，而在“别把结构丢了”，因为一旦图片、表格和上下文断开，召回结果看起来相关，实际很容易答偏。

【OCR 结果你可以理解成“把图片里的字识别出来之后得到的文本”，比如一页 PDF 其实是扫描图，不是真文本，这时候要先用 OCR 把标题、正文、表格里的字尽量识别出来，后面才能拿这些内容去做索引和召回；

rerank 你可以理解成“二次排序”，因为第一轮检索通常只是先粗召回一批可能相关的内容，但这批结果里相关性高低不一定准，所以我会再用一个更强的排序模型，结合 query 和候选 chunk 重新打分，把最相关的几个排到前面。简单说，OCR 解决的是“看得见文档里写了什么”，rerank 解决的是“这么多召回结果里到底哪个最该给模型看”。】

### 不同领域文档特征和结构不一样，是怎么做的？（看自己的项目领域，客服场景的工单，企业知识库的文档）

不同领域文档结构不一样，我一般不会用一套规则硬切，像知识文章比较适合按标题和段落切；如果是客服工单，我会更关注**问答对、问题描述、处理过程、结论**这些字段，优先按**业务字段切**；如果是企业知识库文档，通常会按**章节、条款、表格、附件说明这些结构切，**必要时还会给不同类型文档配不同的解析模板。核心思路就是先识别文档结构，再按领域特征切分和建索引，而不是所有文档都粗暴按固定长度切。

## 向量维度选型？

真正要考虑的是，维度越高，理论上表达能力可能更强，但索引体积、检索延迟、内存成本也会上去，所以在线上我更看重性价比。比如客服工单、企业知识库这类中文检索场景，如果模型本身在 768 或 1024 维效果已经稳定，我通常不会为了追一点点召回率去上更高维度，除非评测里确实看到长文本、多术语、跨表述召回有明显收益。

我的做法一般是先定候选模型，再用业务真实 query 做离线评测，看 recall、mrr、nDCG 和线上延迟、索引成本，最后选一个效果够用、成本可控的维度。

向量维度**不是越大越好**，要在**语义表达能力、存储成本、检索速度**之间做平衡；

RAG 场景主流选 **768 / 1024 维**，小模型轻量场景用 **384 维**，极少用到 1536 以上。

# 检索流程

## 上下文构建如何构建？

 一般采用 Redis + uid 构建，上下文长可以让 AI 概况（某个用户当前会话需要用到的上下文，按 uid 或者 uid + session_id 作为 key 存到 Redis 里。比如一个用户连续问了 5 轮，你不可能每次都从数据库慢慢拼，所以会把最近几轮问答、用户身份、业务状态、已命中的知识点、摘要 memory 先缓存到 Redis，下次这个用户再来请求时，系统先根据 uid 把这份上下文拿出来，再补上本轮输入，一起送给模型。它本质上不是 Redis 帮你“理解上下文”，而是 Redis 帮你快速按用户维度管理短期会话状态，让上下文读取更快、状态更连续）



上下文构建这块，我一般分成**会话态上下文**和**知识检索上下文**两层做。会话态这层，Redis + uid/session_id 很常见，存**最近几轮对话**、**用户画像**、**当前任务状态**、**上一步工具结果**这些短期记忆；如果上下文太长，不会把全量历史都塞给模型，而是做滚动摘要，把已经稳定的信息总结成一段 memory，只保留最近几轮原文。

知识检索这层，不直接把召回结果整包丢进去，而是先做 query 改写、意图识别、关键词补全这些检索前优化，再用混合检索把候选内容捞出来；**通俗来说**（用户的问题不能直接拿去查，先“加工一下”再查会更准。比如用户问“那个报销怎么弄”，这句话太短也太口语化，系统一般会先判断他想问的是“差旅报销流程”还是“发票报销规则”，再补全成更适合检索的话，比如“员工差旅费用报销流程、审批节点、所需材料”；然后再去知识库里同时用关键词和语义检索找内容。这样做比直接拿“那个报销怎么弄”去搜，命中的结果会稳很多。）

###  检索前后做过什么优化？

检索后会做去重、rerank、按主题聚合、按时间或权限过滤，最后只挑和当前问题最相关的几段拼进 prompt。优化的是“别把没必要的东西也塞进上下文”。

## RAG 调参 

召回率（本来应该被找出来的正确内容，系统到底找回来了多少。比如知识库里有 10 条内容真的和用户问题相关，但你的检索系统只找回来了 6 条，那召回率就是 60%。）

### RAG 找回 TopN 选型？怎么设置？

(一次检索到底先拿回多少条候选内容最合适。这里的 TopN 就是按相关度排序)

TopN 本质上是在**召回率和噪声之间找平衡**，不是越大越好。一般我会分两层看

第一层是粗召回的 TopN，可以稍微放大一点，比如先召回 20、50 甚至 100 条，目的是别漏掉真正相关的内容；

第二层是送给大模型前的最终 TopN，这个通常会收得比较小，比如 3 到 8 段，因为上下文太多反而会稀释重点。

设置通常不是靠经验，而是拿真实业务问题做评测，观察 TopN 从 5、10、20、50 往上加时，**召回率有没有明显提升**，如果提升已经很小，但无关内容越来越多，那就说明再加没有意义。像客服、知识库这类场景，我一般会先把粗召回放宽，再靠 rerank 和去重压缩，最后只保留最相关的少量内容给模型，这样整体效果比一开始就只查 Top3 要稳。

- [大模型参数如何设置（温度、Top-p、Top-k）+使用场景](https://zhuanlan.zhihu.com/p/24484122371)

## Rerank or Re-evaluation 怎么做的？

Rerank 就是先用向量检索快速召回一批文档，再用专门的**重排模型**对问题和文档做交叉打分，按相关性重新排序，提高召回精度。

Re-evaluation 是对重排结果做阈值过滤或大模型评估，过滤掉不相关内容，避免胡说，保证回答质量。（就是对 Rerank 后的结果再做一次 “质检”）

## 有没有用过知识谱图？text-to-sql？

## 如何评估你的 RAG？

至于 RAG 的评估，一般分三层看

第一层看检索，像召回率、MRR、nDCG，判断该找出来的内容有没有找回来、排得靠不靠前；第二层看答案质量，重点看正确率、引用命中率、幻觉率、是否回答完整；

第三层看线上指标，比如首 token 延迟、整体响应时间、用户追问率、人工转接率、满意度



## 召回准确率？有无评估？

- [RAG 青铜升级：知识库召回率优化实战攻略](https://zhuanlan.zhihu.com/p/1932099184925836374)

# 回答生成

## prompt如何调优？

- 设定工作流，设置 AI 回答思路，罗列为 TODO